---
title: Exact Match
description: Check for exact string equality
order: 2
---

# Exact Match Metric

This one's straightforward — it checks if the actual output is identical to the expected output. No fuzzy matching, no partial credit.

## Usage

```python
import fasteval as fe

@fe.exact_match()
def test_exact():
    response = my_function("input")
    fe.score(response, "expected output")
```

## Parameters

| Parameter | Type | Default | Description |
|-----------|------|---------|-------------|
| `normalize` | bool | False | Normalize whitespace before comparison |
| `case_sensitive` | bool | True | Case-sensitive matching |
